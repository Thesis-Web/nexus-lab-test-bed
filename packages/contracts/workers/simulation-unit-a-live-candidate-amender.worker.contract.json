{
  "schema_id": "https://nexus-lab-test-bed.local/schemas/worker-contract.schema.json",
  "worker_id": "worker-simulation-unit-a-live-candidate-amender-v1",
  "title": "Simulation Unit A Live Candidate Amender Worker v1",
  "version": "v1-0-0",
  "allowed_capabilities": [
    "simulation.sql.amend",
    "simulation.sql.review",
    "registry.read"
  ],
  "side_effects": ["file_read", "file_write"],
  "determinism": [
    "Must amend only declared Unit A simulation SQL draft artifacts.",
    "Must not promote SQL into live init surfaces in this phase.",
    "Must not use uncontrolled randomness.",
    "Must not write to AI-control roots."
  ],
  "runtime": {
    "language": "typescript",
    "entrypoint": "apps/workers/simulation-unit-a-live-candidate-amender/src/main.ts",
    "mode": "bounded"
  },
  "input_contract": {
    "type": "object",
    "required_fields": ["amendment_scope", "issued_at"],
    "notes": [
      "Amendment scope is limited to Unit A base table drafts in this phase.",
      "Output remains draft-only."
    ]
  },
  "output_contract": {
    "type": "object",
    "required_fields": ["amendment_scope", "result_status", "amended_files"],
    "notes": [
      "Output is amended draft SQL only.",
      "Any live promotion requires a later explicit owner-approved phase."
    ]
  }
}
